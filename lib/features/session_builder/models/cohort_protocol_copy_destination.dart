/// Where a copied Cohort Protocol Session should be saved.
enum CohortProtocolCopyDestination {
  /// Independent programme-only Session scoped to the current programme version.
  programmeOnly,

  /// Reusable coach Session in Session Library.
  sessionLibrary,
}
