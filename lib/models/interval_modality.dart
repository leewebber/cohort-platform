/// Primary movement context for an interval session.
///
/// Affects pace display defaults and future device export profiles without
/// changing the core execution model.
enum IntervalModality {
  running,
  cycling,
  rowing,
  skiing,
  other,
}
