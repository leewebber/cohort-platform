name: cohort_plan_package
description: Shared pure-Dart Authored Plan Package compiler and contracts.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.2

dependencies:
  crypto: ^3.0.7
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
